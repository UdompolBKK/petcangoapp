# 📚 คู่มือการ Migration ข้อมูล PetCanGo

## 🎯 ภาพรวม

คู่มือนี้จะช่วยคุณย้ายข้อมูลจาก MySQL (OctoberCMS เก่า) ไปยัง Firebase Firestore

**ข้อมูลที่จะย้าย:**
- ✅ สิ่งอำนวยความสะดวก (Facilities) - ~15 รายการ
- ✅ จังหวัด (Provinces) - 31 จังหวัด
- ✅ บทความ (Blogs) - ~30+ บทความ
- ✅ ที่พัก (Hotels) - 550+ ที่พัก

**เวลาที่ใช้:** ประมาณ 5-10 นาที (ขึ้นอยู่กับความเร็วของเครื่อง)

---

## 📋 ขั้นตอนที่ 1: เตรียมความพร้อม

### 1.1 ติดตั้ง Dependencies

```bash
cd /Users/udompolthivakorakot/Desktop/petcangoapp
npm install
```

หรือถ้าใช้ pnpm:
```bash
pnpm install
```

### 1.2 ตรวจสอบ MySQL Server

ตรวจสอบว่า MySQL Server กำลังทำงาน:

```bash
mysql -u root -p
```

ถ้าเข้าได้ แสดงว่า MySQL พร้อมใช้งาน พิมพ์ `exit` เพื่อออก

ถ้าเข้าไม่ได้:
```bash
# macOS (ถ้าติดตั้งผ่าน Homebrew)
brew services start mysql

# หรือ
sudo /usr/local/mysql/support-files/mysql.server start
```

### 1.3 ตั้งค่า Firebase Admin SDK

1. **ดาวน์โหลด Service Account Key:**
   - ไปที่ [Firebase Console](https://console.firebase.google.com/)
   - เลือกโปรเจค `petcangoapp`
   - ไปที่ Project Settings → Service Accounts
   - คลิก "Generate new private key"
   - ดาวน์โหลดไฟล์ JSON

2. **บันทึกไฟล์:**
   ```bash
   # ย้ายไฟล์ไปที่ root ของโปรเจค
   mv ~/Downloads/petcangoapp-xxxxx.json serviceAccountKey.json
   ```

3. **ตรวจสอบว่าไฟล์ถูกต้อง:**
   ```bash
   cat serviceAccountKey.json | head -n 5
   ```
   ต้องเห็น JSON ที่มี `project_id`, `private_key`, etc.

---

## 📋 ขั้นตอนที่ 2: ตั้งค่า MySQL Connection

แก้ไขการตั้งค่าในไฟล์ migration scripts (ถ้าจำเป็น):

**ไฟล์ที่ต้องแก้:**
- `scripts/migrate-facilities.js`
- `scripts/migrate-provinces.js`
- `scripts/migrate-blogs.js`
- `scripts/migrate-hotels.js`

**ค้นหาส่วนนี้ในแต่ละไฟล์:**
```javascript
const mysqlConfig = {
  host: 'localhost',      // แก้เป็น IP ของ MySQL Server (ถ้าไม่ใช่ localhost)
  user: 'root',           // แก้เป็น username ของคุณ
  password: '',           // ใส่ password (ถ้ามี)
  database: 'dsignweb_petallow'  // ชื่อฐานข้อมูล
}
```

---

## 📋 ขั้นตอนที่ 3: รัน Migration

### วิธีที่ 1: รันทั้งหมดพร้อมกัน (แนะนำ)

```bash
npm run migrate:all
```

สคริปต์จะรันตามลำดับ:
1. Facilities
2. Provinces
3. Blogs
4. Hotels

### วิธีที่ 2: รันทีละอัน

```bash
# 1. ย้ายสิ่งอำนวยความสะดวก (~5 วินาที)
npm run migrate:facilities

# 2. ย้ายจังหวัด (~10 วินาที)
npm run migrate:provinces

# 3. ย้ายบทความ (~15 วินาที)
npm run migrate:blogs

# 4. ย้ายที่พัก (~5-10 นาที)
npm run migrate:hotels
```

---

## 📋 ขั้นตอนที่ 4: ตรวจสอบผลลัพธ์

### 4.1 ตรวจสอบใน Terminal

หลังรันเสร็จ จะเห็นข้อความแบบนี้:

```
✅ Migration สำเร็จ! ย้ายข้อมูล 550 รายการ

📋 จังหวัดที่มีที่พักมากที่สุด (Top 10):
  120 ที่ - ชลบุรี
   95 ที่ - กรุงเทพมหานคร
   85 ที่ - เชียงใหม่
   ...

🎉 Migration เสร็จสิ้น!
```

### 4.2 ตรวจสอบใน Firebase Console

1. เปิด [Firebase Console](https://console.firebase.google.com/)
2. เลือกโปรเจค `petcangoapp`
3. ไปที่ **Firestore Database**
4. ตรวจสอบ Collections:

   ✅ **facilities** → ควรมี ~15 documents
   ✅ **provinces** → ควรมี 31 documents
   ✅ **blogs** → ควรมี ~30+ documents
   ✅ **hotels** → ควรมี 550+ documents

### 4.3 ตรวจสอบข้อมูล Sample

คลิกที่แต่ละ document เพื่อดูข้อมูล:

**ตัวอย่าง Province:**
```json
{
  "name": "ชลบุรี",
  "slug": "chonburi",
  "region": "east",
  "regionTh": "ภาคตะวันออก",
  "hotelCount": 120,
  "attractions": [
    {
      "name": "พัทยา",
      "slug": "pattaya",
      "image": "/..."
    }
  ]
}
```

**ตัวอย่าง Hotel:**
```json
{
  "name": "โรงแรมฟอร์เลิฟรีสอร์ท",
  "slug": "...",
  "priceStart": 240,
  "province": {
    "id": "...",
    "name": "ปทุมธานี",
    "slug": "pathum-thani"
  },
  "facilities": [
    {
      "id": "...",
      "name": "WiFi",
      "slug": "wifi"
    }
  ]
}
```

---

## 🔍 การแก้ไขปัญหา (Troubleshooting)

### ❌ ปัญหา: Cannot connect to MySQL

**อาการ:**
```
Error: connect ECONNREFUSED 127.0.0.1:3306
```

**วิธีแก้:**
```bash
# ตรวจสอบว่า MySQL ทำงานอยู่
sudo service mysql status

# เริ่ม MySQL
sudo service mysql start

# หรือถ้าใช้ Homebrew (macOS)
brew services start mysql
```

---

### ❌ ปัญหา: Firebase Admin not initialized

**อาการ:**
```
Error: Could not load the default credentials
```

**วิธีแก้:**
1. ตรวจสอบว่ามีไฟล์ `serviceAccountKey.json`:
   ```bash
   ls -la serviceAccountKey.json
   ```

2. ตรวจสอบว่าไฟล์เป็น JSON ที่ถูกต้อง:
   ```bash
   cat serviceAccountKey.json | python -m json.tool
   ```

3. ตรวจสอบว่าไฟล์อยู่ที่ root ของโปรเจค (เดียวกันกับ package.json)

---

### ❌ ปัญหา: Access denied for user

**อาการ:**
```
Error: Access denied for user 'root'@'localhost'
```

**วิธีแก้:**
1. ตรวจสอบ username และ password ใน `mysqlConfig`
2. ลองเข้า MySQL ด้วย command line:
   ```bash
   mysql -u root -p
   ```
3. ถ้าเข้าได้ แต่สคริปต์ไม่ได้ อาจต้องเปลี่ยน password ใน config

---

### ❌ ปัญหา: Cannot parse JSON

**อาการ:**
```
⚠️ ไม่สามารถ parse tags สำหรับ กาญจนบุรี: ...
```

**วิธีแก้:**
- นี่เป็น warning ไม่ใช่ error
- สคริปต์จะข้ามข้อมูลที่มีปัญหาและใช้ default value
- ตรวจสอบข้อมูลใน Firestore ว่ามีครบหรือไม่
- ถ้าข้อมูลสำคัญ อาจต้องแก้ไขใน MySQL ก่อนแล้ว migrate ใหม่

---

## 📊 สถิติหลัง Migration

หลังจาก migrate เสร็จ คุณจะได้:

| Collection | Documents | Size (โดยประมาณ) |
|------------|-----------|-------------------|
| facilities | ~15 | < 1 KB |
| provinces | 31 | ~50 KB |
| blogs | ~30 | ~100 KB |
| hotels | 550+ | ~2-3 MB |
| **รวม** | **~626** | **~3 MB** |

---

## ✅ Checklist หลัง Migration

- [ ] ตรวจสอบจำนวน documents ใน Firestore ถูกต้อง
- [ ] ตรวจสอบข้อมูล sample แต่ละ collection
- [ ] ตรวจสอบว่า provinces มี `hotelCount` ที่ถูกต้อง
- [ ] ตรวจสอบว่า hotels มี `province` object (populated)
- [ ] ตรวจสอบว่า hotels มี `facilities` array (populated)
- [ ] ภาษาไทยแสดงผลถูกต้อง (ไม่เป็น ???)

---

## 🚀 ขั้นตอนถัดไป

1. **ทดสอบเว็บไซต์**
   ```bash
   npm run dev
   ```
   เปิด http://localhost:3000

2. **ตรวจสอบหน้าต่างๆ:**
   - [ ] หน้าแรก - แสดงที่พักได้ไหม
   - [ ] รายการจังหวัด - แสดง 31 จังหวัดไหม
   - [ ] รายละเอียดจังหวัด - แสดงที่พักในจังหวัดได้ไหม
   - [ ] รายละเอียดที่พัก - แสดงข้อมูลครบไหม
   - [ ] บทความ - แสดงบทความได้ไหม
   - [ ] ค้นหา - ค้นหาได้ไหม

3. **ตั้งค่า Firestore Security Rules**
   - ป้องกันการเข้าถึงข้อมูลโดยไม่ได้รับอนุญาต
   - ดู `firestore.rules` ในโปรเจค

4. **ตั้งค่า Firestore Indexes** (ถ้าจำเป็น)
   - สำหรับ query ที่ซับซ้อน
   - Firebase จะแจ้งเตือนถ้าต้องสร้าง index

5. **ย้ายรูปภาพ** (ถ้าต้องการ)
   - รูปภาพยังอยู่บน server เก่า
   - อาจต้องย้ายไป Firebase Storage
   - หรือใช้ CDN

---

## 📞 ติดต่อขอความช่วยเหลือ

หากพบปัญหาที่แก้ไขไม่ได้:

1. ตรวจสอบ log ข้อผิดพลาด
2. ตรวจสอบว่าทำตามขั้นตอนครบหรือไม่
3. ส่ง error message ให้ทีมพัฒนา

---

**หมายเหตุ:**
- การ migrate นี้ **ไม่ลบข้อมูลใน MySQL**
- คุณสามารถรัน migrate ซ้ำได้ (จะมีข้อมูลซ้ำใน Firestore)
- ถ้าต้องการ migrate ใหม่ ควรลบข้อมูลใน Firestore ก่อน
